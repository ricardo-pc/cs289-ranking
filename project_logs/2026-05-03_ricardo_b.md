# Project Log — 2026-05-03 (session 2) — Ricardo

## What was done

- Implemented `src/models.py` — NCF (Model B) complete
  - Embedding tables: `user_emb`, `item_emb` — shape `(n_entities, emb_dim)`
  - MLP construction loop: Linear → ReLU → Dropout blocks, output layer
  - `_init_weights`: normal init for embeddings, Xavier uniform for MLP layers
  - `forward()`: concatenates `p_u || q_i`, passes through MLP, sigmoid output → `(B,)`
  - `confidence_weighted_bce()`: standalone loss function, clamps predictions, applies WMF weights
    - positives: `c_ui = 1 + alpha * rating`, negatives: `c = 1`

- Implemented `src/train.py` — full training pipeline
  - `parse_args()`: all hyperparameters exposed as CLI flags with defaults
  - `train_one_epoch()`: forward pos+neg separately, build (2B,) tensors, loss + backprop
  - `evaluate()`: NDCG@10 and HR@10 using leave-one-out protocol (He et al. 2017)
    - vectorized: scores all 100 candidates per user in one model call via `repeat_interleave`
    - rank = count of items scoring higher than positive + 1
  - `main()`: loads data, builds eval negatives on full-density data, training loop with checkpoint saving

- Fixed `torch.load` FutureWarning: added `weights_only=True`

## SCF setup completed

- `conda init` + `source ~/.bashrc` to fix CondaError on login node
- Installed PyTorch cu121 wheel on compute node (`shadowfax`, CUDA 12.8 driver — backward compatible)
- Downloaded ml-1m on SCF via `bash download_data.sh`
- Fixed git auth on compute node: switched remote from HTTPS to SSH

## Smoke tests passed

| Run | Density | Epochs | Device | NDCG@10 | HR@10 | Time |
|-----|---------|--------|--------|---------|-------|------|
| Local (MacBook) | 0.2 | 1 | CPU | 0.2432 | 0.4478 | 16.5s |
| SCF (shadowfax) | 0.2 | 1 | CUDA | 0.2424 | 0.4450 | 25.5s |

Note: GPU slower than CPU at small scale — expected. GPU overhead dominates at density=0.2, 1 epoch. Will be much faster for full 20-epoch runs.

## Baseline NCF results (density=1.0, default hyperparameters)

```
python src/train.py --model ncf --density 1.0 --epochs 20 --batch-size 1024 --device cuda
```

| Metric | Value |
|--------|-------|
| Best val NDCG@10 | 0.3944 |
| Best val HR@10 | 0.6834 |
| Best epoch | 19 / 20 |
| Epoch time (A4000 GPU) | ~118s |
| Total runtime | ~39 min |

Observations:
- Loss dropped cleanly 0.796 → 0.456 over 20 epochs
- NDCG plateaued around epoch 15, oscillating 0.392–0.394 in last 5 epochs
- He et al. 2017 reports 0.44–0.51 with tuned hyperparameters — clear room for BO to improve
- 20 epochs is sufficient; early stopping at epoch 15 would likely give same result

## What's next

- `src/evaluate.py` — standalone test-set evaluator that loads saved `.pt` checkpoints
- Model A (MF) — assigned to teammate
- Bayesian Optimization sweep (STAT 238 repo) — 25-30 trials on SCF, tunes emb_dim, mlp_layers, lr, l2, alpha
- Final 5-density sweep with best hyperparameters θ*

# Project Log — 2026-05-08 — Jonas

## What I did
- Implemented `MF` class in `src/models.py` (Model A): dot-product scoring `sigmoid(p_u · q_i)`, same interface as NCF so training loop and evaluation require no changes
- Wired MF into `src/train.py`: added import, replaced `raise NotImplementedError` with MF instantiation under `--model mf`
- Ran full training sweep (20 epochs, CPU) across all 5 density levels
- Uploaded all 5 checkpoints to HuggingFace under `mf/`

## What is working
- MF trains end-to-end with `python src/train.py --model mf --density <d> --device cpu`
- Loss decreases consistently across all density levels; NDCG@10 and HR@10 improve each epoch

## Val results (density sweep, 20 epochs, default hyperparameters)

| Density | Train interactions | Best NDCG@10 | Best HR@10 |
|---------|-------------------|--------------|------------|
| 100%    | 988,129           | 0.3857       | 0.6742     |
| 80%     | 788,113           | 0.3810       | 0.6677     |
| 60%     | 590,457           | 0.3717       | 0.6563     |
| 40%     | 392,864           | 0.3617       | 0.6379     |
| 20%     | 195,208           | 0.3215       | 0.5790     |

Sparsity effect is clear: NDCG drops ~0.06 from 100% to 20% density.

## What is blocked / needs a decision
- Hyperparameters not yet tuned (emb_dim=64, lr=1e-3, alpha=1.0 are defaults) — Bayesian Optimization sweep pending
- These are val results; test-set evaluation via `evaluate.py` not yet run

## What's next
- Run Bayesian Optimization to tune `emb_dim`, `lr`, `l2`, `alpha` for MF
- Run `evaluate.py` on test set with best checkpoint once BO is done
- Compare MF vs NCF results across density levels for the sparsity analysis
